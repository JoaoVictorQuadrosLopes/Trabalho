-- Create DB (run in MySQL)
CREATE DATABASE IF NOT EXISTS financeiro_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- Use financeiro_db;
-- Note: Hibernate (DDL auto=update) will create tables.
